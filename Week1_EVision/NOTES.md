Generated project notes:
- I created a simple training pipeline using RandomForest.
- Assumes target column is named 'price'. Update --target to match your CSV.
- Encodes categorical variables with one-hot encoding (may expand later).
- Saves model to models/rf_model.joblib
